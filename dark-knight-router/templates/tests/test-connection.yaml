apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dark-knight-router.fullname" . }}-test-connection"
  labels:
    {{- include "dark-knight-router.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dark-knight-router.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
