apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bedindustries-microservice-group.fullname" . }}-test-connection"
  labels:
    {{- include "bedindustries-microservice-group.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bedindustries-microservice-group.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
