apiVersion: v1
kind: Service
metadata:
  name: {{ include "dark-knight-microservice.fullname" . }}
  labels:
    {{- include "dark-knight-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dark-knight-microservice.selectorLabels" . | nindent 4 }}
