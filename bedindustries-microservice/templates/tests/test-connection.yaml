apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bedindustries-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "bedindustries-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bedindustries-microservice.fullname" . }}:80']
  restartPolicy: Never
