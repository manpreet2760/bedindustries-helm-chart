{{- if and .Values.persistence.enabled (not .Values.persistence.existingClaim) }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ template "bedindustries-microservice-worker.fullname" . }}
  labels: {{- include "bedindustries-microservice-worker.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.size | quote }}
  {{ include "bedindustries-microservice-worker.storageClass" . }}
{{- end }}
