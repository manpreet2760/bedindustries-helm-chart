apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bedindustries-microservice-worker-storage.fullname" . }}-test-connection"
  labels:
    {{- include "bedindustries-microservice-worker-storage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bedindustries-microservice-worker-storage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
