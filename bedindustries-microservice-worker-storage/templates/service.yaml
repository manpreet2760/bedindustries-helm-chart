apiVersion: v1
kind: Service
metadata:
  name: {{ include "bedindustries-microservice-worker-storage.fullname" . }}
  labels:
    {{- include "bedindustries-microservice-worker-storage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bedindustries-microservice-worker-storage.selectorLabels" . | nindent 4 }}
